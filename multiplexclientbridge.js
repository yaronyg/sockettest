var multiplex = require('multiplex')

exports.clientBridge = function(portForTCPIPClientBridge) {
  /*
  Set up a TCP/IP client connection to the TCP/IP Client Bridge
  Set up a TCP/IP listener
  When a connection is made on the listener then create a new substream
   */
};